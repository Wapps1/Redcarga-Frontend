package com.wapps1.redcarga.features.chat.domain.models

/**
 * Tipo de mensaje (quién lo envió)
 */
enum class ChatMessageType {
    USER,   // Mensaje enviado por un usuario (cliente o proveedor)
    SYSTEM  // Mensaje del sistema
}

